/// <reference types="react-scripts" />

type Track = {
    id: string;
    previewUrl: string;
    name: string;
    artistNames: string[];
    image: string;
};

type RefreshToken = string;
type AccessToken = string;
